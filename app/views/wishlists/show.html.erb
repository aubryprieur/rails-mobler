<div class="page-title">
  <div class="container">
    <div class="column">
      <h1><%= @wishlist.title %></h1>
    </div>
    <div class="column">
      <ul class="breadcrumbs">
        <li>
          <%= link_to "Supprimer la wishlist", wishlist_path(@wishlist), :method => 'delete' %>
        </li>
      </ul>
    </div>
  </div>
</div>
<div class="container">
  <div class="wishlist-description"><h4> <%= @wishlist.description %></h4></div>
  <div class="row">
    <% @furnitures.each do |furniture| %>
    <div class="col-sm-3 margin-bottom-2x pb-2">
      <div class="card"><img class="card-img-top" src="<%= furniture.photo %>" alt="Card image">
        <div class="card-block">
          <h4 class="card-title"> <%= furniture.title %></h4>
          <p class="card-text">
            <strong>L:</strong><%= furniture.length %> cm <strong>H:</strong> <%= furniture.height %> cm <strong>W:</strong> <%= furniture.width %> cm
          </p>
          <div class="row">
            <%= link_to "Delete", furniture_path(furniture), :method => 'delete', class: "btn btn-secondary btn-sm" %>
            <div>
              <button class="btn btn-secondary btn-sm" data-toggle="modal" data-target="#modaldefault" title="Whishlist">Comment</button>
              <% content_for :modals do %>
              <%= render 'comment' %>
              <% end %>
            </div>
            <div>
              <button class="btn btn-secondary btn-sm" data-toggle="modal" data-target="#modalLarge" title="Whishlist">Reviews</button>
              <% content_for :modals do %>
              <%= render 'reviews' %>
              <% end %>
            </div>
          </div>
          <div class="text-center">
            <%= link_to "Buy", furniture.url, target: "_blank", class: "btn btn-outline-primary btn-sm" %>
          </div>
        </div>
      </div>
    </div>
    <% end %>
  </div>
</div>
